{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@10.11.1",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "build": "pnpm run typecheck && PORT=8080 BASE_PATH=/ pnpm --filter \"./artifacts/cucci\" run build && pnpm -r --if-present --filter \"!./artifacts/mockup-sandbox\" --filter \"!./artifacts/cucci\" run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "only-allow": "^1.2.2",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
